# Supabase (public)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Absolute site URL — required for OG images, sitemap, canonical URLs
NEXT_PUBLIC_SITE_URL=https://example.com

# Optional
GITHUB_TOKEN=
GEMINI_API_KEY=

# DB connection for migrations (psql) — never commit the real value
DIRECT_DATABASE_URL=
